namespace CodeOps.Domain.Abstractions
{
    /// <summary>
    /// Marker abstraction for domain events. Extend with timestamp / correlation data if needed.
    /// </summary>
    public interface IDomainEvent { }
}
